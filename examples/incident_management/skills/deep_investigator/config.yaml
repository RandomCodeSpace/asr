description: Diagnostic deep-dive — pull logs, metrics, propose hypotheses
kind: responsive
tools:
  local: ["get_logs", "get_metrics", "submit_hypothesis"]
routes:
  - when: success
    next: resolution
    gate: confidence
  - when: failed
    next: __end__
  - when: default
    next: resolution
    gate: confidence
