export default function App() {
  return (
    <div style={{ padding: 32, fontFamily: 'system-ui' }}>
      <h1>ASR React UI · v2.0.0-rc1 (scaffold)</h1>
      <p>Tokens + components land in subsequent tasks (12-20).</p>
    </div>
  );
}
