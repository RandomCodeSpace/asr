description: Perform diagnostic deep-dive — pull logs, metrics, propose hypotheses
tools:
  local: ["get_logs", "get_metrics", "update_incident"]
routes:
  - when: success
    next: resolution
    gate: confidence
  - when: failed
    next: __end__
  - when: default
    next: resolution
    gate: confidence
